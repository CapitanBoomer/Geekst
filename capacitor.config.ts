import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'La Geek Cueva',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
